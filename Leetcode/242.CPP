#include<bits/stdc++.h>
using namespace std;
bool solver(string t, string s){
    if(t.size()!=s.size()) return false;
   set<char> sett,sets;
   for(int i=0;i<t.size();i++){
    sett.insert(t[i]);
    sets.insert(s[i]);
   }
   
}
int main(){
   string t="anagram";
   string s="nagaram";
}